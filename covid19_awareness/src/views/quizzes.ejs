<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Covid 19 awareness</title>
    <link rel="stylesheet" href="/css/styles.css">
    <style>
    .header_special {
    color: #fff;
    background-color: #10555A;
    margin-bottom: 10px;
  }
  .button {
      border: none;
    color: white;
    padding: 12px 30px;
    text-align: center;
    text-decoration: none;
    display: inline-block;
    font-size: 16px;
    margin: 4px 2px;
    transition-duration: 0.4s;
    cursor: pointer;
    background-color: white;
    color: black;
    border: 2px solid #4CAF50;
  }
  .alignRight {
      text-align: right;
  }
  label:after {
   content: "Choose your location:";
   color: white;
  }

</style>
</head>
<body style="background-color: #FCF0CF;">
      <header class="header_special">
        <title>Covid 19 Quiz Game</title> 
      </header>
    <div class="wrapper" >
      <div class="quizzes">
        <a href="/">&larr;- Back</a>
        <div>
          <table style="width:1200px" border="0">
            <% for (i =0; i<3; i++){ %>
                  <tr>
                    <td style="text-align: center; font-size: 26px;"><%= quizzes[i].title %></td>
                  </tr>
                  <tr>
                    <td style="text-align: center;"><%- quizzes[i].description %></td>
                  </tr>
                  <tr>
                    <td style="text-align: center;"><a href="/quizzes/<%- quizzes[i].path%>/00" class="button">Take the Quizz</a></td> 
                  </tr>
            <% } %>
            <% for (i =0; i < addQuiz.length; i++) { %>
                <tr>
                    <td style="text-align: center; font-size: 26px;"><%= addQuiz[i].title %></td>
                  </tr>
                  <tr>
                    <td style="text-align: center;"><%- addQuiz[i].description %></td>
                  </tr>
                  <tr>
                      <td style="text-align: center;"><a href="/quizzes/<%- addQuiz[i].path%>/<%= i %>/00" type="submit" class="button"  value="<%= i %>" name = "quesPosi">Take the Quizz</a></td> 
                  </tr>
            <% } %> 
          </table>
          <% if(request == "set") { %>
            <a href="/quizzes/addNew" class="button" style="text-align: left;">Add new Quiz question</a></td> 
            <% } %>
      </div>
    </div>
  </div>
</body>

</html>